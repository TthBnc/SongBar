# SongBar 0.1.3

## What's fixed

- Fixed the share button so the macOS share picker opens on the first click.
- Clicking the share button again now closes the active share picker instead of opening another one.
- Replaced the README panel image with an actual current SongBar screenshot.

## Notes

- This build is still unsigned. On first launch, right-click SongBar in Applications and choose Open.
